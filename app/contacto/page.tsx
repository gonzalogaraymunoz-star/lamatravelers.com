import Planner from '@/components/Planner';
import { site } from '@/lib/site';
export const metadata={title:'Contacto'};
export default function Page(){return <><section className="page-hero simple"><div className="eyebrow">Contacto</div><h1>Cuéntanos tu viaje.<br/>No necesitas tenerlo resuelto.</h1><p>Con fechas aproximadas, pasajeros y una idea de lo que te interesa podemos empezar.</p><div className="contact-actions"><a className="button button-dark" href={site.salesWhatsAppUrl} target="_blank" rel="noreferrer">WhatsApp ventas</a><a className="button button-outline" href={`mailto:${site.email}`}>Escribir por correo</a></div></section><section className="section contact-layout"><div><Planner/></div><aside><div className="eyebrow">También puedes escribir</div><a className="contact-big" href={site.salesWhatsAppUrl} target="_blank" rel="noreferrer">WhatsApp ventas</a><a className="contact-big" href={`mailto:${site.email}`}>{site.email}</a><p>{site.location}</p><p className="muted">Antes de confirmar una reserva verificamos disponibilidad, modalidad, condiciones de la experiencia y política aplicable.</p></aside></section></>}
